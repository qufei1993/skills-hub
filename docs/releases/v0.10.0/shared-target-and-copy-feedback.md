# 共享安装目录与设备码反馈

- 同一 Skill 的多个工具共用目录时只替换一次文件，同一事务更新所有 copy 记录及基线。
- 不同 Skill 的活动记录占用同一路径时仍拒绝覆盖；保留真实编辑保护及回滚机制。
- 复制设备码成功后显示勾选与成功文字，2 秒后恢复；失败显示安全提示并允许重试。不展示异常原文。
- 回归覆盖共享记录更新、跨 Skill 拒绝覆盖、本机编辑保护、异步复制成功、失败与重试。
